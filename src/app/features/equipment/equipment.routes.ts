import { Routes } from '@angular/router';
import { EquipmentListComponent } from './components/equipment-list/equipment-list.component';

export const EQUIPMENT_ROUTES: Routes = [{ path: '', component: EquipmentListComponent }];
