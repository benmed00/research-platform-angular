import { Routes } from '@angular/router';
import { SpeciesListComponent } from './components/species-list/species-list.component';

export const SPECIES_ROUTES: Routes = [{ path: '', component: SpeciesListComponent }];
