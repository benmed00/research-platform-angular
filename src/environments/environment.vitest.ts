import { AppEnvironment } from './environment.types';

/** Used when running unit tests (optional file replacement in angular.json). */
export const environment: AppEnvironment = {
  production: false,
  apiUrl: '/api'
};
