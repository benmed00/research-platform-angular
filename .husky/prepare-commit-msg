# Remove Cursor co-author trailers; Jacobe-Son is the only allowed co-author.
MSG_FILE="$1"
if [ -f "$MSG_FILE" ]; then
  sed -i.bak '/Co-authored-by: Cursor/d' "$MSG_FILE" 2>/dev/null || sed -i '' '/Co-authored-by: Cursor/d' "$MSG_FILE"
  rm -f "${MSG_FILE}.bak"
fi
